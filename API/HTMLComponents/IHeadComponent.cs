namespace NetDotNet.API.HTMLComponents
{
    // Empty interface representing anything that can be put in a <head> tag but not a <body> tag
    public interface IHeadComponent : IHTMLComponent
    {

    }
}
